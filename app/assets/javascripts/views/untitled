var App = window.App != null ? window.App : {};

App.ThanksView = Backbone.View.extend({
  initialize: function() {
    _.bindAll(this, 
      'render'
    );
  },

  render: function() {
    var view = new App.ThanksView();
  }
});